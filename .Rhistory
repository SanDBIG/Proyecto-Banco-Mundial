subtitle = "Chile, Argentina y Uruguay, 2003-2019",
x = "Año",
y = "Score",
color = "País"
) +
theme_minimal()
if (input$mostrar_valores) {
p <- p +
geom_text(
aes(label = round(Value, 1)),
size = 3,
vjust = -0.8,
na.rm = TRUE,
show.legend = FALSE
)
}
p
})
}
shinyApp(ui = ui, server = server)
shinyApp(ui = ui, server = server)
shinyApp(ui = ui, server = server)
ui <- fluidPage(
# Título general del dashboard.
titlePanel("Indicadores seleccionados en países del Cono Sur"),
# ---------------------------------------------------------------------------
# FILA 1: BOTONERA / CONTROLES
# ---------------------------------------------------------------------------
# fluidRow() crea una fila horizontal.
fluidRow(
# width = 12 ocupa todo el ancho de la pantalla.
# Shiny divide la pantalla en 12 columnas.
column(
width = 12,
# wellPanel() crea el cuadro gris donde van los controles.
# padding controla el espacio interno del cuadro.
# margin-bottom agrega separación respecto de la fila siguiente.
wellPanel(
style = "padding: 8px; margin-bottom: 10px;",
# Fila interna para distribuir los controles dentro del cuadro gris.
fluidRow(
# Control 1: rango de años.
# width = 4 ocupa un tercio de la botonera.
column(
width = 4,
sliderInput(
inputId = "rango_anios",
label = "Rango de años",
min = 2003,
max = 2019,
value = c(2003, 2019),
step = 1,
sep = ""
)
),
# Control 2: selección de países.
# width = 5 ocupa un poco más que el slider.
# inline = TRUE deja las opciones en línea horizontal.
column(
width = 5,
checkboxGroupInput(
inputId = "paises",
label = "Países",
choices = c("Argentina", "Chile", "Uruguay"),
selected = c("Argentina", "Chile", "Uruguay"),
inline = TRUE
)
),
# Control 3: mostrar u ocultar etiquetas de valores.
# width = 3 completa la fila.
# 4 + 5 + 3 = 12.
column(
width = 3,
checkboxInput(
inputId = "mostrar_valores",
label = "Mostrar valores",
value = TRUE
)
)
)
)
)
),
# ---------------------------------------------------------------------------
# FILA 2: TABLA Y GRÁFICOS
# ---------------------------------------------------------------------------
# Esta fila contiene tres cuadros principales:
# tabla resumen + gráfico permisos + gráfico starting.
fluidRow(
# Cuadro 1: tabla.
# width = 4 significa que ocupa 1/3 de la pantalla.
# Para achicar la tabla, usa width = 3.
# Para agrandarla, usa width = 5 o 6.
column(
width = 2,
h4("Tabla resumen"),
DTOutput("tabla_resumen")
),
# Cuadro 2: gráfico de permisos de construcción.
# width = 4 significa que ocupa 1/3 de la pantalla.
# height controla el alto del gráfico.
column(
width = 5,
h4("Construction permits"),
plotOutput("grafico_permisos", height = "430px")
),
# Cuadro 3: gráfico de Starting a business.
# width = 4 significa que ocupa 1/3 de la pantalla.
# La suma 4 + 4 + 4 = 12 completa una fila.
column(
width = 5,
h4("Starting a business"),
plotOutput("grafico_starting", height = "430px")
)
)
)
server <- function(input, output) {
# ---------------------------------------------------------------------------
# TABLA RESUMEN
# ---------------------------------------------------------------------------
output$tabla_resumen <- renderDT({
DT::datatable(
tabla_conosur_resumen,
rownames = FALSE,
filter = "top",
options = list(
# Número de filas visibles por página.
# Como la tabla ocupa 1/3 de la pantalla, conviene dejar pocas filas.
pageLength = 6,
# Permite desplazamiento horizontal si hay muchas columnas.
scrollX = TRUE,
# Controla qué elementos aparecen en la tabla.
# f = filtros
# t = tabla
# i = información inferior
# p = paginación
# Se excluye "l" para no mostrar selector de cantidad de filas.
dom = "ftip"
)
)
})
# ---------------------------------------------------------------------------
# GRÁFICO 1: CONSTRUCTION PERMITS
# ---------------------------------------------------------------------------
output$grafico_permisos <- renderPlot({
# Se usa data_conosur ya construido.
# Solo se filtra dinámicamente según los controles del dashboard.
p <- data_conosur %>%
filter(
`Series Name` == "Dealing with construction permits: Time (days)",
`Country Name` %in% input$paises,
Year >= input$rango_anios[1],
Year <= input$rango_anios[2]
) %>%
ggplot(aes(
x = Year,
y = Value,
color = `Country Name`,
group = `Country Name`
)) +
# Línea temporal por país.
geom_line(linewidth = 0.9, na.rm = TRUE) +
# Puntos sobre cada año.
geom_point(size = 1.8, na.rm = TRUE) +
# Eje X dinámico según el rango seleccionado.
scale_x_continuous(
breaks = seq(input$rango_anios[1], input$rango_anios[2], 2)
) +
# Títulos y etiquetas.
labs(
title = "Dealing with construction permits: Time (days)",
subtitle = "Chile, Argentina y Uruguay, 2003-2019",
x = "Año",
y = "Días",
color = "País"
) +
# Estética base del gráfico.
theme_minimal()
# Si el usuario activa "Mostrar valores",
# se agregan etiquetas numéricas con un decimal.
if (input$mostrar_valores) {
p <- p +
geom_text(
aes(label = round(Value, 1)),
size = 3,
vjust = -0.8,
na.rm = TRUE,
show.legend = FALSE
)
}
# Devuelve el gráfico final.
p
})
# ---------------------------------------------------------------------------
# GRÁFICO 2: STARTING A BUSINESS
# ---------------------------------------------------------------------------
output$grafico_starting <- renderPlot({
# Se usa data_conosur ya construido.
# Solo se filtra dinámicamente según los controles del dashboard.
p <- data_conosur %>%
filter(
`Series Name` == "Starting a business - Score",
`Country Name` %in% input$paises,
Year >= input$rango_anios[1],
Year <= input$rango_anios[2]
) %>%
ggplot(aes(
x = Year,
y = Value,
color = `Country Name`,
group = `Country Name`
)) +
# Línea temporal por país.
geom_line(linewidth = 0.9, na.rm = TRUE) +
# Puntos sobre cada año.
geom_point(size = 1.8, na.rm = TRUE) +
# Eje X dinámico según el rango seleccionado.
scale_x_continuous(
breaks = seq(input$rango_anios[1], input$rango_anios[2], 2)
) +
# Títulos y etiquetas.
labs(
title = "Starting a business - Score",
subtitle = "Chile, Argentina y Uruguay, 2003-2019",
x = "Año",
y = "Score",
color = "País"
) +
# Estética base del gráfico.
theme_minimal()
# Si el usuario activa "Mostrar valores",
# se agregan etiquetas numéricas con un decimal.
if (input$mostrar_valores) {
p <- p +
geom_text(
aes(label = round(Value, 1)),
size = 3,
vjust = -0.8,
na.rm = TRUE,
show.legend = FALSE
)
}
# Devuelve el gráfico final.
p
})
}
shinyApp(ui = ui, server = server)
ui <- fluidPage(
# Título general del dashboard.
titlePanel("Indicadores seleccionados en países del Cono Sur"),
# ---------------------------------------------------------------------------
# FILA 1: CONTROLES DEL DASHBOARD
# ---------------------------------------------------------------------------
fluidRow(
# width = 12 hace que la botonera ocupe todo el ancho del dashboard.
column(
width = 12,
# wellPanel crea el cuadro gris de controles.
# padding controla el espacio interno.
# margin-bottom separa visualmente la botonera de la tabla.
wellPanel(
style = "padding: 8px; margin-bottom: 10px;",
# Fila interna para distribuir los controles dentro de la botonera.
fluidRow(
# Control del rango de años.
# width = 4 ocupa un tercio de la botonera.
column(
width = 4,
sliderInput(
inputId = "rango_anios",
label = "Rango de años",
min = 2003,
max = 2019,
value = c(2003, 2019),
step = 1,
sep = ""
)
),
# Control de selección de países.
# width = 5 ocupa un poco más de espacio horizontal.
# inline = TRUE deja las opciones en una misma línea.
column(
width = 5,
checkboxGroupInput(
inputId = "paises",
label = "Países",
choices = c("Argentina", "Chile", "Uruguay"),
selected = c("Argentina", "Chile", "Uruguay"),
inline = TRUE
)
),
# Control para mostrar u ocultar etiquetas numéricas en los gráficos.
# width = 3 completa la fila.
# 4 + 5 + 3 = 12.
column(
width = 3,
checkboxInput(
inputId = "mostrar_valores",
label = "Mostrar valores",
value = TRUE
)
)
)
)
)
),
# ---------------------------------------------------------------------------
# FILA 2: TABLA RESUMEN HORIZONTAL
# ---------------------------------------------------------------------------
fluidRow(
# width = 12 hace que la tabla ocupe todo el ancho disponible.
column(
width = 12,
h4("Tabla resumen"),
# div permite controlar el tamaño vertical de la tabla.
# max-height define el alto máximo del contenedor.
# overflow-y permite scroll vertical si la tabla excede ese alto.
# margin-bottom separa la tabla de los gráficos.
div(
style = "max-height: 220px; overflow-y: auto; margin-bottom: 15px;",
DTOutput("tabla_resumen")
)
)
),
# ---------------------------------------------------------------------------
# FILA 3: GRÁFICOS
# ---------------------------------------------------------------------------
fluidRow(
# width = 6 hace que el primer gráfico ocupe la mitad izquierda.
# height controla el alto del gráfico en pantalla.
column(
width = 6,
h4("Construction permits"),
plotOutput("grafico_permisos", height = "500px")
),
# width = 6 hace que el segundo gráfico ocupe la mitad derecha.
# 6 + 6 = 12, por eso ambos quedan en una misma fila.
column(
width = 6,
h4("Starting a business"),
plotOutput("grafico_starting", height = "500px")
)
)
)
server <- function(input, output) {
# ---------------------------------------------------------------------------
# TABLA RESUMEN
# ---------------------------------------------------------------------------
output$tabla_resumen <- renderDT({
DT::datatable(
tabla_conosur_resumen,
rownames = FALSE,
filter = "top",
options = list(
# Número de filas visibles en la tabla.
# Como la tabla está dispuesta horizontalmente y angosta,
# conviene mostrar pocas filas.
pageLength = 3,
# Activa scroll horizontal para no deformar el dashboard
# cuando hay muchas columnas.
scrollX = TRUE,
# Alto interno de la tabla.
# Si quieres una tabla más alta, sube este valor.
# Si quieres una tabla más angosta, bájalo.
scrollY = "120px",
# Controla qué elementos muestra la tabla.
# f = filtros
# t = tabla
# i = información inferior
# p = paginación
# Se excluye "l" para ocultar el selector de número de filas.
dom = "ftip"
)
)
})
# ---------------------------------------------------------------------------
# GRÁFICO 1: CONSTRUCTION PERMITS
# ---------------------------------------------------------------------------
output$grafico_permisos <- renderPlot({
# Se usa data_conosur ya construido.
# Aquí solo se filtra dinámicamente según los controles del dashboard.
p <- data_conosur %>%
filter(
`Series Name` == "Dealing with construction permits: Time (days)",
`Country Name` %in% input$paises,
Year >= input$rango_anios[1],
Year <= input$rango_anios[2]
) %>%
ggplot(aes(
x = Year,
y = Value,
color = `Country Name`,
group = `Country Name`
)) +
# Línea temporal por país.
geom_line(linewidth = 0.9, na.rm = TRUE) +
# Puntos por año.
geom_point(size = 1.8, na.rm = TRUE) +
# Eje X dinámico según el rango de años seleccionado.
scale_x_continuous(
breaks = seq(input$rango_anios[1], input$rango_anios[2], 2)
) +
# Etiquetas generales del gráfico.
labs(
title = "Dealing with construction permits: Time (days)",
subtitle = "Chile, Argentina y Uruguay, 2003-2019",
x = "Año",
y = "Días",
color = "País"
) +
# Tema visual base.
theme_minimal()
# Si el usuario activa "Mostrar valores",
# se agregan etiquetas numéricas con un decimal.
if (input$mostrar_valores) {
p <- p +
geom_text(
aes(label = round(Value, 1)),
size = 3,
vjust = -0.8,
na.rm = TRUE,
show.legend = FALSE
)
}
# Devuelve el gráfico final.
p
})
# ---------------------------------------------------------------------------
# GRÁFICO 2: STARTING A BUSINESS
# ---------------------------------------------------------------------------
output$grafico_starting <- renderPlot({
# Se usa data_conosur ya construido.
# Aquí solo se filtra dinámicamente según los controles del dashboard.
p <- data_conosur %>%
filter(
`Series Name` == "Starting a business - Score",
`Country Name` %in% input$paises,
Year >= input$rango_anios[1],
Year <= input$rango_anios[2]
) %>%
ggplot(aes(
x = Year,
y = Value,
color = `Country Name`,
group = `Country Name`
)) +
# Línea temporal por país.
geom_line(linewidth = 0.9, na.rm = TRUE) +
# Puntos por año.
geom_point(size = 1.8, na.rm = TRUE) +
# Eje X dinámico según el rango de años seleccionado.
scale_x_continuous(
breaks = seq(input$rango_anios[1], input$rango_anios[2], 2)
) +
# Etiquetas generales del gráfico.
labs(
title = "Starting a business - Score",
subtitle = "Chile, Argentina y Uruguay, 2003-2019",
x = "Año",
y = "Score",
color = "País"
) +
# Tema visual base.
theme_minimal()
# Si el usuario activa "Mostrar valores",
# se agregan etiquetas numéricas con un decimal.
if (input$mostrar_valores) {
p <- p +
geom_text(
aes(label = round(Value, 1)),
size = 3,
vjust = -0.8,
na.rm = TRUE,
show.legend = FALSE
)
}
# Devuelve el gráfico final.
p
})
}
shinyApp(ui = ui, server = server)
# Carga de librerias
source("02_R/01_libraries.R")
# Carga de librerias
source("02_R/01_libraries.R")
# Carga de librerias
source("02_R/01_libraries.R")
# Carga de datos
source("02_R/02_load_data.R")
# Data limpia y creación de formato long
source("02_R/03_clean_data.R")
# Exploratory data analysis all variables
source("02_R/04_eda.R")
# Creacion de tablas y graficos estaticos
source("02_R/06_graphs-visuals.R")
# Creacion de dashboard shiny
source("02_R/08_Dashboard.R")
# Creacion de quarto
source("")
# Carga de librerias
source("02_R/01_libraries.R")
# Carga de datos
source("02_R/02_load_data.R")
# Data limpia y creación de formato long
source("02_R/03_clean_data.R")
# Exploratory data analysis all variables
source("02_R/04_eda.R")
# Creacion de tablas y graficos estaticos
source("02_R/06_graphs-visuals.R")
# Creacion de dashboard shiny
source("02_R/08_Dashboard.R")
shinyApp(ui = ui, server = server)
